var goldBars = 0
func unlockTreasureChest(inventory: Int) -> Int {
    // since the function is one line of code we can remove the return statement
    inventory + 100
}

goldBars = unlockTreasureChest(inventory: goldBars)
print(goldBars)

goldBars = unlockTreasureChest(inventory: goldBars)
print(goldBars)

goldBars = unlockTreasureChest(inventory: goldBars)
print(goldBars)
